spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kdd
    username: kdd
    password: kdd
  jpa:
    show-sql: true

app:
  cors:
    allowed-origins: http://localhost:3000
    secure-cookie: false
